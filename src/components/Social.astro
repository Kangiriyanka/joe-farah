---
// Props: platform: string, username: string
const { platform, username, image } = Astro.props;
---
<a href={`https://www.${platform}.com/${username}`}>
  <img src = {image} alt= {platform}>
</a>

<style>

  img {
    background-color:
    width: rem;
    height: 2rem;
  }

  
</style>